# AGENTS.md

## 项目目标

本项目的最终宗旨是持续采集多个交易场所（包括 CEX 和 DEX）的公开行情数据，为后续检查跨市场、现货/合约和链上/链下套利机会提供可靠的历史输入。

数据来源包括：

- CEX 的现货、永续及交割合约盘口；
- 订单簿型 DEX 的订单簿或成交簿；
- AMM 型 DEX 的池子状态、流动性分布和可执行报价。

项目的当前边界是**采集、标准化、校验、存储和查询历史行情**。除非用户明确要求，不实现交易执行、私钥使用、资金归集或自动套利下单。

## 架构原则

采集链路必须保持以下分层：

```text
交易所/链节点适配器
  -> 标准化器
  -> 内存盘口或 AMM 报价状态
  -> 每秒采样器
  -> 分钟快照与秒级差量写入器
  -> 查询/回放接口
```

- 适配器只处理各数据源的协议差异；不要把业务分析规则写进适配器。
- 标准化器输出统一的 `instrument_id`、买卖方向、整数价格 tick、整数数量 lot 和 UTC 时间。
- 写入器只保存前 50 档的每秒状态：每分钟完整快照一次，分钟内保存秒级差量。
- 查询端负责把一分钟快照和对应差量恢复为任意秒的盘口。

完整的数据模型以 [docs/market-data-storage.md](docs/market-data-storage.md) 为准。

## 不可违反的数据不变量

1. 时间统一存 UTC；界面或报告中再转换时区。
2. 价格和数量不得使用二进制浮点数。必须保存为整数 `price_tick` 和 `qty_lot`。
3. `instrument_id` 必须区分交易所、市场类型、标的、结算币种及合约版本；`BTCUSDT` 字符串本身不是唯一交易流。
4. CEX 订单簿必须按交易所序列号或更新范围校验连续性。发生断档时，重新获取快照；不能静默沿用旧状态。
5. 分钟第 0 秒保存完整的买卖各 50 档；第 1 至 59 秒仅保存相对本分钟上一个有效采样状态的最终差量。连续有效时即相对前一秒；无效区间后的首个有效秒相对上一个有效状态。
6. 差量以价格为键，不以“第几档”为键。数量为 0 表示该价格从保存的前 50 档状态删除。
7. `valid_bitmap` 必须区分“盘口无变化”和“该秒数据无效/缺失”。
8. DEX 数据必须记录可复现的链上位置：至少区块高度，且对可能重组的链记录区块哈希和最终性状态。
9. AMM 没有天然的买一至买五十。必须按文档中固定的价格/报价规则生成合成深度，不能伪装成原始限价订单簿。
10. 不提交 API key、私钥、RPC 凭据、真实账户数据或 `.env` 文件。

## CEX 与 DEX 规范化

- CEX 和订单簿型 DEX：维护本地 L2 状态，按价格排序后截取买卖各 50 档。
- AMM 型 DEX：保存可复现池状态，并按固定的交易方向、报价货币、价格精度和采样规则计算 50 个合成深度档位。
- 跨市场比较前必须统一基础币数量、报价币价值、合约乘数、手续费口径和 DEX gas 成本；不得直接比较原始 `qty_lot`。

## 数据库与性能约束

- 不将每个价位拆成常驻的独立完整快照行。
- 不在热路径中存 JSON、字符串价格或 `Float64`。
- 分钟快照表按 `(instrument_id, minute_time)` 查询；分钟内差量表按 `(minute_id, second_offset)` 查询。
- 只有在实测证明按价格的历史检索成为瓶颈时，才增加专用辅助索引；索引必须说明其额外空间成本。
- 写入应按批次进行，避免一条 WebSocket 消息一次数据库写入。

## 实现与测试要求

涉及数据采集或恢复逻辑的改动，至少验证：

1. 完整快照加全部差量可精确恢复一分钟内每一秒的前 50 档；
2. 同一秒同一价格的多次源更新只保留采样时刻的最终数量；
3. 数量为 0 的删除操作正确生效；
4. 无变化秒与无效秒可通过 `valid_bitmap` 区分；
5. CEX 序列断档后不会生成看似有效的盘口；
6. DEX 重组或未最终确认区块不会与最终数据混淆；
7. 对代表性的活跃和非活跃交易流测量每日压缩后占用和查询耗时。

修改表结构、编码规则或标准化语义时，同步更新 `docs/market-data-storage.md`。
